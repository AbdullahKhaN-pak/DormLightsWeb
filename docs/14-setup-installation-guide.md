# 14 - Local Setup & Installation Guide

## Quick Start (One Command)

1. Ensure Node.js (v18+) is installed on your system.
2. Open terminal in the project directory:
   ```bash
   cd "/Users/abdullah/Desktop/Dorm Lights"
   ```
3. Run the development server:
   ```bash
   npm run dev
   ```
4. Access the web application:
   - **Storefront**: `http://localhost:3000`
   - **Admin Panel**: `http://localhost:3000/admin`
   - **API Backend**: `http://localhost:3001/api/products`

---
*Delivered for DormLights PRD Section 11.*
